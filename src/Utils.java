/**
 * Utils.java
 * This class will provide Utility Methods, Enumerate types and Constants for the whole project package
 */
public class Utils {
    // ComparisonOperator is an enumerate for > < = >= <= operorters, this will be used for filtering data in certain range
    public static enum ComparisonOperator {
        Less, LessOrEqual, Equal, Larger, LargerOrEqual
    }
}
